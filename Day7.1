import random
word_list = ["aadvaark", "camel", "baboon"]
chosen_word = random.choice(word_list)
dash="_"
output=[]
n=len(chosen_word)
guessed_letter = input("Guess a letter ").lower()
is_correct=False

print(chosen_word) #Result verification

for letter in chosen_word:
    if guessed_letter==letter:
        output.append(guessed_letter)
        is_correct=True
    else:
        output.append(dash)

if is_correct==True:
    print("RIGHT")
else:
    print("WRONG")

print("The output is ", ''.join(output))
